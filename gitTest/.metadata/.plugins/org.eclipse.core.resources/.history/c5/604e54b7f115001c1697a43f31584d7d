package 상속기초;

public class Child extends Parent {
	
	//자식클래스(서브클래스)
	
	// 1. 새로운 기능 추가
	public void makeSam() {
		System.out.println("삼겹살을 굽는다.");
	}
	// 2. 기본클래스(부모클래스)가 가지고 있던 기능을 재정의 
	// 메소드 오버라이딩(재정의)
	// - 상속이 되어있따는 전제하에 부모클래스가 가지고 있는 메소드를
	// 그대로 가지고와서(리턴타입, 메소드명, 매개변수 동일){}(body)안에 있는
	// 로직만 다시 정의하는것
	public void makeBibim() {
		System.out.println("맛있는 육회비빔밥을 만든다~");
	}
	
}
